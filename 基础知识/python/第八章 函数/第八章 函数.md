1.函数实参
  1.位置实参
   2.关键字实参

```plain
函数调用（形参名='实参',形参名='实参'）
```
2.传递任意数量实参
  *相当于建立一个元组来收集所有值

```plain
def pizza(*a)
```
3.**参数 接受任意数量的关键字形参
4.函数别名 （模块等方面使用）
两个不同文件函数别名
```plain
from piza import pizza as mp
mp('five','big')

def pizza(size,kind):
    print(f"this {kind} of {size} of pizz is sold out")
    
```
5.as给模块指定别名
piza=p
import piza as p
p.pizza('size','kind'）
