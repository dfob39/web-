1.字符串中插入变量值，在双引号前加上f，将要插入的变量放在大括号内
eg.
f字符串使用
```python
number_jb=number.pop(0)
print(f"i am sorry for {number_jb}")
```
2.变量两端空白删除
左端 listrip   右端 rstrip  两端 rstip
eg.

```plain
a=' python '
b=a.rstrip
print(b)
```
3.全大写字母表示常量，值始终不变
